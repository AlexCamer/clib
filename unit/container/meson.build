# stack
project_units += executable(
    'stack',
    'stack.c',
    include_directories : project_includes,
    link_with : container_library
)

# vector
project_units += executable(
    'vector',
    'vector.c',
    include_directories : project_includes,
    link_with : container_library
)
